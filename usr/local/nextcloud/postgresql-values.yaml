global:
  postgresql:
    auth:
      postgresPassword: kjldf25
      username: nextclouduser
      password: fhgDD27
      database: nextcloud

primary:
  persistence:
    enabled: true
    storageClass: nextcloud
    size: 20Gi
    accessModes:
      - ReadWriteMany

service:
  type: ClusterIP

architecture: standalone

metrics:
  enabled: false

resources:
  requests:
    cpu: 200m
    memory: 512Mi
  limits:
    cpu: 200m
    memory: 512Mi

